# Run with: nc2zarr -vv -c aviso_monthly.yml
# When providing a list of directories use the "-s name" option

input:
  paths:
     - /home/b/b382076/scratch/aviso/1993/??/dt*2024.nc

#  decode_cf: true
  multi_file: true

process:
  rechunk:
    '*':
      lon: null  # no chunking for lon dim in all variables
      lat: null  # no chunking for lon dim in all variables
      time: 30    # chunk size 1 for time dim in all variables
    lon: null    # no chunking for lon dim in lon variable
    lat: null    # no chunking for lat dim in lat variable
    time: 1

output:
  path: /home/b/b382076/scratch/aviso/aviso_test.zarr
  overwrite: false
  consolidated: true
  append_dim: time