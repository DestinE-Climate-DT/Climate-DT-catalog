sources:
  # test-ng5:
  #   description: 5km experiment, coupuled with IFS
  #   driver: yaml_file_cat
  #   args:
  #       path: "{{CATALOG_DIR}}/test-ng5.yaml"
  test-pi:
    description: 5km experiment, coupled with IFS
    driver: yaml_file_cat
    args:
        path: "{{CATALOG_DIR}}/test-pi.yaml"
  hpz3:
    description: hpz3 5 years data from FESOM nextgems4 
    driver: yaml_file_cat
    args:
        path: "{{CATALOG_DIR}}/hpz3.yaml"