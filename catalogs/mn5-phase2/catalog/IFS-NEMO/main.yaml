sources:
  a1g5:
    description: IFS/NEMO - SSP3-7.0 projection - Tco399/eORCA025
    driver: yaml_file_cat
    args:
      path: '{{CATALOG_DIR}}/a1g5.yaml'
  t01o:
    description: IFS/NEMO test run for AQUA workflow integration
    driver: yaml_file_cat
    args:
      path: '{{CATALOG_DIR}}/t01o.yaml'
